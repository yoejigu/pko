name: pko
description: A minimal TypeScript Pulumi program that deploys a DB today
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript
